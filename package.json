{
  "name": "arkhet",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install-server": "curl -fsSL https://bun.sh/install | bash && bun i",
    "start-server": "cd packages/backend && bun start"
  }
}
