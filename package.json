{
  "name": "arkhet",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install-server": "docker pull oven/bun && docker run --rm --init --ulimit memlock=-1:-1 oven/bun && bun i",
    "start-server": "cd packages/backend && bun start"
  }
}
