{
  "name": "arkhet",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install-server": "powershell -c \"irm bun.sh/install.ps1|iex\" && bun i",
    "start-server": "cd packages/backend && bun start"
  }
}
